node_modules/
.next/
out/
cloudflared.exe
assets/_*.log
assets/_*.png
.claude/
_pagesim/

# Dropbox crawl / matching scratch data
scripts/out/

# secrets — never commit
.env
.env.*
!.env.example

# internal docs — repo is public, these stay local only
docs/

# collector runtime output (archived as GitHub Actions artifacts instead)
data_collection/snapshots/
